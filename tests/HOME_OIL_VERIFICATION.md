# 油画背景与首页布局验证（2026-09-16）

## 首页按钮后续调整

首页删除「关于」入口；换图与主题按钮取消共用底色条及边框，统一 20px 图标与 44px 点击区。桌面/手机两套主题截图通过；键盘切换主题、上传打开裁剪、Escape 取消与焦点恢复通过，内页仍有「关于」。首次上传验证使用较大草地图时未在 5 秒内出现裁剪器，独立重跑小图并以 15 秒超时验证通过。构建、Go 单测及 diff 检查通过；DESIGN lint 无错误，静态审计仍为上述 21 个既有事件绑定识别误报。

## 变更

- 亮色主页接入 NGA 高清《撑伞的女人》，暗色沿用自托管《睡莲》。新素材出处见 `web/static/img/monet-parasol-SOURCES.md`。
- 移除独立通栏，改为连续整页画面；标题与应用入口独立排布。桌面保留右侧原画，手机保留上方主体。
- 全站笔触底纹由亮色 4.5% / 暗色 2.8% 调整为 16% / 12%，亮色底纹使用无人草地，避免人物重复。
- 背景上传按钮移入页头，保留既有裁剪、存档和恢复默认逻辑。

## 检查结果

- `go build -o bin/homepage .`、`go test ./...`、`git diff --check` 通过。
- `/tmp/verify-home-oil.py`：1440×900、1920×800、390×844、320×568、820×1180、844×390 共 12 组尺寸/主题检查通过；无横向溢出，标题与导航可见。
- 桌面与手机两主题的小游戏集合、AI 对话、关于及 2048 共 16 个内页检查通过。
- 实际操作通过：亮暗切换及刷新保存、常规动效与减少动态偏好、上传/裁剪/保存/刷新、恢复默认、Escape 取消与焦点恢复、主页与小游戏往返。没有控制台或页面错误。
- 截图和 JSON：`/tmp/home-oil-verification/`。检查为 Chromium 模拟视口，不等同于物理手机或 Safari 验收。
- DESIGN.md 官方 lint：0 errors、12 warnings（已有未引用镜像 tokens 与非标准组件说明字段）。
- Premium strict 静态审计仍报告 21 个 actionless-button，与变更前 `/tmp/monet-ui-audit.json` 数量相同。该规则无法跨文件识别原生 JS 事件绑定；本次触及的主题按钮由 common.js 绑定，背景按钮由 nav.js 绑定，均已用浏览器实际点击验证。未为消除静态误报改写无关游戏或聊天逻辑。报告 `/tmp/home-oil-audit.json`。
