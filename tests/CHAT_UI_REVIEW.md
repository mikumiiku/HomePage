# AI 对话界面审查（2026-09-11）

依据用户截图，在 1536 × 774 CSS 像素、深色主题下，用不含真实密钥的 hello 对话复现并对比。

| 问题 | 修改前 | 修改后 |
| --- | --- | --- |
| 消息区被输入区挤压 | 消息区高 306px | 高 472px |
| 输入框默认过高 | 172px | 110px；长输入自动增高 |
| 消息区与输入框不对齐 | 1151px / 900px | 统一 880px |
| 短消息气泡占满阅读区 | hello 气泡宽 648px | 按内容收缩至约 66px，靠右 |
| 底部提示重复占行 | 完成状态与快捷键两行 | 状态通过 live region 播报；手机隐藏快捷键 |
| 流式输出重建消息节点 | 全部消息 replaceChildren | 按消息 ID 更新变化分区，保留已完成正文、思考节点和焦点 |
| 布局变化导致跟随失效 | 同步滚动，未处理输入区变化 | 布局完成后同步；ResizeObserver 校正；主动上翻暂停跟随 |
| 设置内容挤出关闭操作 | 整个弹窗滚动 | 固定标题和完成按钮，内容单独滚动 |

验证：

- `CHAT_BASE=http://127.0.0.1:8036 python3 tests/chat_layout_test.py`：通过。短回复完整可见，长流式回复保持展开和焦点，上翻不被拉回、回到底部恢复跟随，输入增高、亮暗模式、320px 窄屏、长设置表单和新对话清理。
- `CHAT_BASE=http://127.0.0.1:8036 python3 tests/chat_test.py`：通过。模型配置、CORS 直连、分块 SSE、Markdown/XSS、停止、重试、IME、导入导出、本机持久化和标签页冲突。
- `CHAT_BASE=http://127.0.0.1:8036 python3 tests/chat_connection_test.py`：通过。自动模型列表、五档思考参数、多连接、失败保留、超时取消、旧存档迁移、超长模型名和主题。
- JavaScript 语法检查与 Go 构建通过；`go test ./...` 成功，项目无 Go 测试文件。
- Premium 静态审计仍报告 21 条 `affordance.actionless-button`：审计器未识别项目使用 `$()` 查找元素的外部事件绑定，且含其他页面的既有条目。本次没有把静态审计标为通过；聊天相关按钮已通过上述真实浏览器交互验证。

边界：使用本地模拟模型服务，不使用用户密钥。验证 Chromium 的桌面与窄屏布局；未在实体 iOS/Android 设备上验证软键盘。
