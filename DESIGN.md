---
version: alpha
name: 主页
description: 个人应用入口与小游戏集合，采用真实油画笔触与低对比页面底纹。
colors:
  primary: "#306838"
  background: "#e7f0ef"
  surface: "#f7faf4"
  text: "#21392c"
  secondary: "#4c6d70"
  sky: "#64a8d6"
  gold: "#bfa64f"
typography:
  sans:
    fontFamily: 'PingFang SC, Microsoft YaHei, sans-serif'
  display:
    fontFamily: 'Noto Serif CJK SC, Songti SC, SimSun, serif'
rounded:
  sm: 8px
  md: 14px
  lg: 24px
spacing:
  page-max: 1240px
  card-gap: 16px
components:
  button:
    min-height: 44px
  card:
    padding: 28px
  dialog:
    max-height: 92dvh
---

# 主页设计约定

## 方向与产品背景
用户明确偏好莫奈的印象派笔触和《撑伞的女人》的配色：明亮蓝天、乳白云层、黄绿草地、深绿阴影和少量金色。油画感来自有方向的笔触和颜色交叠；禁止用重复 SVG feTurbulence 噪点模拟画布，也不使用平面几何弧线代替油画。个人工具入口与小游戏以中文内容清晰、手机和 iPad 显示稳定为优先。站点名称「主页」，集合名称「小游戏」。

## 背景与表面
自托管 monet-meadow.png 是参考《撑伞的女人》笔触和色彩生成的原创草地天空画面，并非原作复刻。主页展示完整画面，白色标题与底部遮罩保证可读性；深色模式叠加 painting-tone 降低画面亮度。页面共用单张固定视口底图，通过 painting-opacity 在浅色 12%、深色 5.5% 呈现柔和笔触，不重复平铺，不随长页面拉伸。聊天页底纹再降至公共透明度的 65%；卡片和表单保留实色以维持可读性。
底图 URL 在 layout.html 使用二进制版本指纹提供给 --oil-painting；home.css 是背景透明度、主题遮罩与界面色彩的唯一 owner。沿用从莫奈画作取色的原有界面 tokens、字体、操作色、布局和自定义图片流程。图片与生成提示词来源见 web/static/img/monet-meadow-SOURCES.md。

## 运行时映射
`web/static/css/home.css` 顶部 `:root` 与 `html[data-theme="dark"]` 是颜色唯一运行时来源；本文镜像核心值，不生成第二套变量。primary 对应 pond，background 对应 wall，surface 对应 card，text 对应 ink，secondary 对应 ink-soft，sky 对应 skywater，gold 对应 ochre。深色使用蓝绿背景、柔和油画底纹与浅绿操作色；所有新增表面、阴影、滚动条颜色均通过这两个 token 块管理。

## 排版与布局
标题用系统宋体族，正文与控件用系统无衬线中文字体，无远程字体。1240px 页面容器，流式内边距；主页默认是油画背景，也支持上传图片，游戏列表双列，560px 以下单列。正文保持自然滚动；画作预留宽高，游戏舞台不得超出容器。圆角层级为按钮 8px、列表 14px、首屏 24px。阴影仅用于悬停、画框与浮层。

## 游戏页标题与游戏列表
游戏页不再隐藏一级标题：`game-ui.js` 把 `.game-title-row`（图标 + `h1.game-heading`）连同「全部游戏」返回入口一起移入公共页头，页面始终有可见且可访问的游戏名；560px 以下隐藏标题前的小图标并降到 15px。小游戏集合页按设备过滤，只列出适合当前平台的游戏，不提供跨平台入口，因此不写「全部游戏」之类的数量或范围承诺，也不显示数量（站点文案、meta description 与关于页一律不写具体个数）。旧版 `.device-filter / .filter / .auto-note` 筛选器样式已删除。

## 可读性底线
扫雷数字（`--mine-n*`）与 2048 方块（`--t2048-*`）在两套主题下都必须对各自底色达到 4.5:1；2048 的 tile2/tile4 用 `--ink` 配字，其余方块底在两套主题下相同，字色固定为 `--t2048-on-8` / `--on-tile`，不做深色覆盖。深色模式下禁止用通配的 `.cell2048` / `.mine-cell` 规则覆盖带状态类的同一元素——那会静默吃掉方块色阶与插旗底色，需要限定 `:not(.tile)` 或补 `.flagged` 同特异度规则。设备不适配提示 `.notice` 文字用 `--ink`（底色为 `--hero-surface`）。

## 错误页
404 与其他页面共用同一套 layout（`web/templates/notfound.html`）：保留页头品牌、关于入口、主题按钮与页脚署名，颜色全部来自 token 并跟随亮暗模式；正文只给「页面不存在」的事实与返回主页／小游戏两个出口，不用 emoji 当图标。`renderNotFound` 先渲染到缓冲区再写 404 状态码，避免模板出错时状态码与内容不一致。

## 触控目标、开关状态与辅助文本
所有可点控件（含主页换背景按钮、移动端导航项）触控高度不低于 44px；页头品牌是文字标题链接，只保证 24×24 以上，不为了凑 44px 抬高页头。开关类按钮用固定名称 + `aria-pressed` 表达状态（主题按钮名「亮暗模式」，`aria-pressed` 为是否暗色；游戏内暂停、旗子模式、瞄准、蹲伏同理），`title` 可以写动作。只给辅助技术读的文本统一用 `home.css` 的 `.sr-only`（游戏内播报区同源）。

## 覆盖层、状态播报与确认弹窗（core 框架契约）
游戏不得自行拼 DOM 覆盖层：`App.overlay(stage, opts)` 是唯一入口。它同一舞台只保留一个覆盖层（出现前先清旧的，避免结算卡叠在新一局上），产出 `role="dialog"` + `aria-modal`，有标题时用 `aria-labelledby`、有文案时用 `aria-describedby`，无标题时用 `opts.label`（难度选择传「选择难度」，纯开局用首个动作名兜底）。出现时焦点进入首个动作按钮，Tab 在覆盖层内循环，关闭后焦点归还触发元素，无法归还时落到 `#stage`（`tabindex="-1"`，不画焦点框）。暂停类覆盖层传 `opts.onEscape` 让 Esc 直接继续（俄罗斯方块、沙城突击）。`App.clearOverlays(stage)` 供各游戏开局时清理。

`App.gamePaused()` 同时覆盖「有原生 dialog 打开」「有覆盖层」「手动暂停」三种情况，游戏计时与输入一律以它为准；因此依赖 Esc/按键恢复的暂停逻辑必须把该键处理放在 `gamePaused()` 判断之前。

状态播报分两档：离散事件（失误、暂停/继续、配对结果、重开）用 `App.announce(text, true)` 立即播报；分数、最佳、计分标签这类高频变化不逐次播报，由 `App.hud.*` 写入后按 5 秒节流合并成一句（播报区是 body 下懒建的 `.sr-only[role=status]`）。`#hud` 是 `role="group" aria-label="对局状态"`，辅助技术可随时主动读取；计时类数值只提供可读标签，不进入播报区。HUD 芯片里放按钮时用 `.hud-chip:has(button)` 去掉下划线与基线对齐，避免开关看起来像指标。

游戏页的一级标题由 `game-ui.js` 移入公共页头；`game-ui.js` 还必须清掉五子棋／围棋自己的 `gm-side-info` / `go-side-info`——那两个类由各游戏 `fitBoard` 的首次 ResizeObserver 回调添加，可能早于统一布局生效，残留会留下一个 280px 空列让棋盘左偏（同一页面每次加载结果不同）。

会丢进度的动作（2048 重新开始）用 `App.confirm({title, copy, confirmLabel, cancelLabel, onConfirm})`：原生 dialog，Escape 关闭、焦点约束，关闭后焦点归还触发元素。

## 公共组件与交互
公共 owner：layout.html 管理导航，home.css 管理控件与焦点，common.js 管理主题与提示，nav.js 管理背景裁剪。Bootstrap Icons 本地 SVG mask 用于 UI；游戏继续使用现有 game-icons mask。主按钮深绿实底，次按钮描边；原生链接负责导航。游戏按请求设备自动显示，不提供设备选择，不读取旧 platform cookie 或 URL 参数。所有控件保留键盘焦点，触屏可直接操作；减少动态偏好关闭动画。滚动条全局使用 thumb/track/hover/active tokens，强制色交由系统。

## 背景设置
选择本地文件 → 裁剪弹窗 → 保存应用 → 留在主页并提示成功。取消与 Escape 均关闭并恢复焦点，使用原生 dialog 提供焦点约束与背景隔离；图片读取失败提供文字反馈。默认与上传背景均不显示说明面板；默认和上传背景都使用暖白标题与底部渐暗遮罩保证可读性。游戏进度与主题沿用既有本地槽位。

## 验证
构建 Go 嵌入静态资源。检查主页、集合、关于、游戏页，覆盖亮暗主题、手机窄屏、筛选导航、游戏启动与背景裁剪。没有既有前端测试框架，浏览器证据放在临时目录，避免测试依赖进入项目。


## 主页标题导航过渡
主页不渲染页头品牌链接；图片左下角的 h1 与内页页头品牌链接均包含唯一 `.home-wordmark`。同源跨文档 View Transitions 使用共享名称，将 52–80px 标题平滑移动缩放为 24px 品牌；返回首页时反向执行，页面内容同时淡入淡出。不拦截链接、不替换 DOM，不改变游戏脚本生命周期及浏览器历史。减少动态效果时禁用；不支持的浏览器正常导航。标题不再执行初次加载浮现动画，避免与导航快照冲突。

导航动效时长：标题移动与缩放 1000ms，旧页淡出 450ms，新页淡入 750ms；保持现有缓动曲线。

界面文案保持必要信息：页头导航只保留「关于」，应用区只保留「小游戏」标题入口，不显示介绍性宣传文案、自动识别提示或开发过程说明。页脚只保留图标署名。


## 沙城突击
用户要求增加 CS 风格 3D 枪战，定位为原创场景的单人三轮清场训练，不包含联网对战。沿用公共导航、按钮、HUD、覆盖层与存档；游戏舞台扩展到 1180px。沙色庭院、拱门、木箱与细节枪械承担视觉重点，界面使用系统中文无衬线字体；时间与轮次位于正上方，生命在左下，弹药及纵向枪械剪影在右下；战术地图仅在按住 Tab 时显示。
运行时 `home.css` 中 `--fps-*` tokens 同时驱动场景材质和 HUD；亮色为日光沙城，深色为蓝灰暮色，`M.theme.isDark()` 调整灯光，并监听 themechange 重绘。Three.js 0.160.1、图标与 Poly Haven CC0 真实颜色/法线纹理全部自托管；纹理不可用时保留程序材质。游戏采用独立 `.fps-stage` 样式变体，公共覆盖层只覆盖训练场，不阻止页面导航。
电脑提供鼠标锁定与拖动回退、WASD 移动、方向键瞄准和空格射击；手机提供移动/射击按钮与拖动视角，建议横屏。失焦、退出鼠标锁定与切换标签页自动暂停，继续操作恢复进度；减少动态偏好关闭走路晃动。WebGL 不可用、资源加载失败或图形上下文丢失均提供明确提示和重新加载按钮。

沙城突击战术扩展：人物采用带贴图的骨骼士兵（独立 Idle / Walk 混合），第一人称展示独立枪械和手套手臂。主武器在开始或暂停时通过原生 select 选择，接受系统弹出菜单样式；右下角纵向装备栏切换主武器／手枪／刀／手雷，使用 aria-pressed 标明当前持握。所有弹药分武器维护，切换不补弹；暂停保留拉栓、装填和手雷引信。C 切换蹲伏；视点降至 1.02m、移动减慢，遮挡使用实际视点。触屏提供蹲伏、瞄准与投雷按钮。新增 gun / steel / rubber 材质颜色均由 home.css 的两套主题 tokens 管理。狙击镜限定在视口内；全屏游玩时收起主武器选择栏，在暂停时恢复。

## AI 对话（浏览器直连）
采用本站原生页面与 Marked / DOMPurify 自托管库，无独立前端框架、模型代理、账号或服务端存储；保留 /home 入口。参考 Anse 的本地对话思路，不引入其服务端。最高约束来自用户：模型配置、密钥、对话只能保存在浏览器。通过明确的「连接」且获取模型成功后将密钥持久化到 app:chat 槽位，密码框默认遮蔽；导出只含对话，不含连接配置。模型请求仅直达用户配置的外部接口，禁止本站主机、禁止跳转与 cookies，不提供代理回退。服务商必须允许 CORS。
布局按用户 2026-09-10 提供的截图：全宽画布，左侧独立圆角面板保留对话列表与底部设置，右侧空态宋体标题「有什么想聊的？」居中，并在下方放置宽输入区。页头仅显示「主页」与主题切换，不显示游戏署名；移除推荐话题。输入区左下直接显示模型与思考等级两个选择框，设置入口位于侧栏底部，导入导出位于设置弹窗。深色用蓝灰底、浅绿按钮与细边框；浅色使用蓝绿与云白。home.css 的 --chat-wall / --chat-panel / --chat-rule 分别引用公共 wall / card / rule，随主题同步更新，仅在聊天页消费。模型选择与已保存配置明确采用原生 select，系统弹出菜单外观可接受；模型列表由连接成功后的 /models 响应自动填充，不提供手填 ID。手机历史是同一区域切换，主页面不新增强制满屏或 body overflow。页面一级标题是 `.chat-app` 下常驻的 `<h1 class="chat-sr">AI 对话</h1>`，与断点、空态、历史视图切换都无关；侧栏头部只放 `<h2>` 与手机端关闭按钮，桌面端整体不渲染。
Canonical UI Map：导航 layout.html；颜色/字体/焦点/滚动条/btn 为 home.css；主题与成功提示 common.js；存储为 store.js / schemas.js；表单为原生带 label 控件与聊天业务校验；弹窗沿用背景裁剪的原生 dialog 模式，焦点约束、Escape 与关闭恢复；删除、导入与丢弃草稿使用 chat-confirm 业务变体。连接与偏好保存成功后留在设置，显示模型数量或保存反馈；点击完成关闭并恢复焦点。失败保留输入与原有配置，切换未保存连接先确认。消息正文使用安全 Markdown，禁用外部图片与嵌入内容；外部链接仅 http(s)，不发送 referrer。
流式回复支持分块 SSE、中文解码、停止与错误重试，保留中断文本。向上翻阅时不抢滚动，提供回到最新回复；中文输入法回车不发送。历史与草稿自动保存本机；容量不足、存储被禁和另一标签页写入都明确提示，拒绝静默覆盖。响应式、亮暗、空态、完整对话、配置错误、CORS 失败、停止、重试与模型列表加载均以浏览器验证。

## 既有沙城突击交互补充
保留沉浸式 HUD：右下角模型生成的装备剪影，Tab 战术地图、Q 上一武器；开始与暂停菜单在场景内，操作说明默认折叠。狙击拉栓恢复正常视角并将枪移至右下，不遮挡中央。第一人称与世界分两次绘制，防止地面视野染色枪械。静态建筑按材质合并，保留原墙体用于碰撞；世界使用同源真实材质、渐变天空、市场摆设、棕榈、弹痕、弹壳和倒地反馈。
玩家普通视距 26 米、狙击开镜 48 米；探索记忆仅在本局保留，墙后敌人、视野线与警报效果不泄漏。AI 地面视野裁切墙体，延迟广播最后目击位置，队友分路搜索；最多三名同时进攻。后坐力影响真实相机/弹道，连射积累，停火恢复稳定性，蹲伏和瞄准减轻。敌弹 220 米/秒采用完整路段碰撞。敌人撤退与侧移仍注视威胁，调查路线刷新，14 秒位置记忆与到达后 3 秒观察；按武器听觉距离和墙体衰减调查静态声源，刀不产生枪声。

## Canonical UI Map

| Capability | Canonical owner | Source of truth | Allowed variants | Verification |
| --- | --- | --- | --- | --- |
| Select/Listbox | Native select | DESIGN.md | 系统菜单；模型按连接分组；思考等级使用原始英文值 | tests/chat_test.py |
| Form | 原生 label/input 与 chat.js 业务校验 | DATA_MODEL.md | 配置保存与保留错误输入 | tests/chat_test.py |
| Scrollbar | home.css | DESIGN.md | chat-scroll 局部高度 | tests/chat_test.py |
| Toast | common.js App.toast | DESIGN.md | 保存、复制、导入导出 | tests/chat_test.py |
| Dialog | 原生 dialog | DESIGN.md | 设置、确认删除、放弃修改 | tests/chat_test.py |
| CRUD | App.store 与 chat.js | DATA_MODEL.md | 本地配置与对话 | tests/chat_test.py |

AI 对话比例：聊天页容器上限 1800px，桌面侧栏 240px（中等屏幕 200px）；页头压缩为 80px，对话标题、消息阅读区和输入框统一 880px 宽。空态保留居中欢迎语；对话态输入框默认一行，按内容增高至 140px，底部成功状态只由辅助技术播报，不占额外一行。用户短消息靠右按内容宽度展示；正文、思考过程、操作按钮使用明确的字号层级。720px 以下切换手机历史视图，模型、思考等级与发送按钮保持一行，隐藏键盘快捷键提示。body 不锁滚动。


## 日夜整页过渡
主题按钮触发 720ms 整页透明度合成，采用先缓入后缓出的曲线。旧主题快照保持完全不透明，新主题在其上渐入，避免同时淡出导致背景闪白；文字位置与尺寸固定，不加模糊、缩放或额外色罩，背景、面板与游戏 canvas 同步过渡。主题切换暂时取消品牌的独立快照，页面导航仍使用既有标题移动。连续点击保留最终目标，当前过渡结束后平滑接续；系统减少动态偏好与不支持 View Transitions 的浏览器即时切换。App.theme.set 保持同步语义与 themechange 事件，本地存档结构不变。

AI 设置采用单列分区：模型服务（Base URL/API Key/连接状态）、折叠对话偏好、对话备份。名称可选并默认使用域名；密钥默认遮蔽，连接时验证并保存，连接中可关闭或编辑取消；旧响应不得覆盖新输入或其他标签页的数据。底部模型菜单保留「连接模型服务…」入口；思考等级默认不发参数，五个显式等级原样传入 reasoning_effort。选择和错误行为沿用原生 select 与 chat-alert，旧存档迁移由 schemas.js 独立负责。验收包含 tests/chat_connection_test.py 的连接、五档参数、失败保留、取消竞态、超长模型名、主题和 v1 迁移。

2026-09-11 对话界面审查：修正消息与输入区宽度、过高输入框及重复状态提示。按消息 ID 保留 DOM，仅更新变化的消息分区；已结束回复、思考过程节点及键盘焦点不得随每个流式分块重建。滚动在布局完成后跟随，向上滚动或展开思考过程暂停跟随，回到底部恢复；输入区及视口变化由 ResizeObserver 重新校正。设置使用固定标题和完成按钮，独立内容滚动容器，窄屏长表单不遮住退出操作。tests/chat_layout_test.py 覆盖短回复完整可见、流式阅读位置、焦点和展开状态、长输入、主题与窄屏，以及新对话状态清理。

## 五子棋

入口 `/game/gomoku`，15×15 无禁手五子棋，支持人机和同屏双人，手机默认直接落子。沿用公共导航、按钮、原生 select（接受系统弹出菜单）、dialog 与主题；棋盘用 DOM/CSS，单一 Tab 入口、方向键导航和中文坐标播报。棋盘是视觉主体，按用户要求保留站点页头（主页、关于、主题切换）和「全部游戏」返回入口的原始位置与样式。下方以棋盘为主体，双方信息与棋盘等宽；新局设置和本局操作常驻，桌面放右侧 280px 操作栏，手机放棋盘下方，只有显示偏好、棋谱、规则与战绩保留在设置弹窗。新局区域用分隔线和说明与当前对局操作区分，原生 select 保留系统弹出菜单。棋盘取消固定宽度上限，桌面随可用高度放大，手机优先用满内容宽度；空间不足自然滚动，不能为了单屏缩小交叉点或隐藏操作。常规手机三项新局选项并排，360px 以下逐行显示以保留完整文字，短横屏采用侧栏内部滚动。仅游戏舞台可突破站点内容容器宽度，公共页头和返回入口保持原始对齐。不会使用新的字体或远程素材。

`home.css` 的 `--gomoku-*` 双主题 tokens 是棋盘、网格、黑白棋子及获胜线的唯一颜色来源，由 `gomoku.css` 消费；棋子不因主题交换黑白含义。最后一手有圆点或手数与外圈，建议落点有加号和虚线，不只依赖颜色。本游戏使用 100dvh 与完整 flex/min-height 链占满显示区，ResizeObserver 同时处理屏幕旋转、窗口缩放与临时操作条，桌面常规对局页面无滚动，手机按内容自然滚动。该布局只作用于 is-gomoku，其它页面继续自然滚动。拖动/滑动/多指/取消触摸不落子；减少动态效果关闭本模块动画。

规则 owner 为 gomoku-engine.js，电脑搜索为 gomoku-ai.js / gomoku-worker.js，界面控制为 gomoku.js。Worker 故障提供分时快速搜索；旧任务按对局与请求标识隔离。原生确认弹窗只用于丢弃未完成棋局，支持 Escape 与关闭恢复焦点。普通离开页面自动保存，无额外阻止。胜负保留在双方信息区；棋谱、规则、战绩和显示偏好位于原生设置 dialog，固定标题及「完成」按钮，Escape 与关闭恢复设置按钮焦点。开始新局直接打开丢弃当前棋局的确认弹窗，取消恢复新局按钮。悔棋、提示、复盘、新局均可直接操作，手机操作后将棋盘滚入视野。仅确认模式或复盘期间显示必要的棋局操作条，并同步缩放桌面棋盘。设置只对新局生效，确认落子和手数显示即时生效。图标署名保留在设置底部，主题按钮仍在公共页头。

存档遵守 DATA_MODEL.md 的独立 v1 槽位。损坏和未来版本保留原数据并切换临时对局，可导出原存档；另一标签页更新时暂停操作，加载最新进度后继续；存储失败保留当前局并明确提示。验证由 tests/gomoku_engine_test.cjs 和 tests/gomoku_test.py 覆盖。

## 围棋

入口 `/game/go`，支持 9×9、13×13、19×19 的离线人机与同屏双人。采用中国规则：黑先、白贴 7.5 目、数子法、禁止自杀与全局同形再现。双方连续停一手后进入死子核对；人机由玩家确认，同屏双人由黑白依次确认，修改死子会重置双方确认。可以继续对局、悔棋或认输，不提供提示与棋钟。人机悔棋撤回玩家最近一手及其后的电脑回应，同屏双人撤回一手；计分核对阶段也可撤回停一手，终局后不可悔棋。

棋盘是唯一视觉重点：浅色使用暖金 `--go-board` 与深绿 `--go-grid`，深色变为暮色橄榄绿；黑白棋子含义不随主题交换。棋盘画框和棋子允许轻微阴影，其余操作区继续使用公共 card / rule / btn 体系。桌面以大棋盘和 280px 操作栏并列，手机自然滚动；触屏 13/19 路默认先选点，以 5×5 局部放大核对后确认，9 路直接落子。触控落点以按下时实际命中的网格单元为准，允许轻微手指抖动，但滚动、多指与系统取消不会落子。颜色只由 home.css 的 `--go-*` 两套主题 token 提供。

规则 owner 为 go-engine.js，休闲棋力搜索为 go-ai.js / go-worker.js，界面与存档控制为 go.js。电脑提供轻松、标准、深入三档约 200/800/2200ms Worker 搜索，故障后切换为可取消的分时快速搜索。存档遵守 `go:main` v1，损坏或未来版本保留原字符串并允许临时对局，跨标签页冲突暂停写入。验证由 tests/go_engine_test.cjs、tests/go_test.py 与 go_game_test.go 覆盖。

## 国际象棋
沿用主页绿系、宋体标题、无衬线控件及现有棋类侧栏结构。棋盘是视觉中心：传统黑白 SVG 棋子、浅绿与深绿方格，选中格、上一步与合法落点用不同形状区分；王被将军时另有文字反馈。桌面棋盘与 304px 侧栏并排，760px 以下上下排列并自然滚动。保留页头、返回入口和「国际象棋」标题，不改变其他游戏布局。运行时颜色唯一来源为 home.css 的 --chess-* 亮暗 tokens，棋子为自托管原始美术资源。
Canonical UI Map：导航/标题模板 game.html；按钮/主题/焦点/滚动条 home.css 与 common.js；存档 schemas.js/store.js；确认与升变复用项目原生 dialog 模式；对战方式/难度/执棋使用原生 select，接受系统弹出菜单。棋盘业务使用原生按钮与单个 Tab 入口，方向键导航，点击或键盘走子。棋谱内部限高滚动，侧栏与页面保持自然高度。
人机三档离线 Worker 搜索，不标注虚构等级分；双人同屏。休闲规则自动判三次重复与五十回合和棋，规则面板明确说明。自动续局，终局棋谱可查看及导出；开始新局和认输沿用棋类确认方式。存档损坏或未来版本保留原值并提供备份，跨标签页冲突冻结并允许加载最新进度。


## 全部小游戏单屏布局（2026-09-14）
本节按最新用户要求替代前述游戏页自然滚动、常驻新局参数的约定。十个游戏共用 game-ui.css / core/game-ui.js：显示区使用 100dvh 与完整 flex/min-height 链，游戏按剩余宽高等比放大；棋盘左右居中，宽屏对称预留即时操作位置，窄屏操作位于棋盘下方。设置弹窗独立滚动，保留可达的完成按钮与 Escape / 焦点恢复。
即时操作为悔棋、提示、停一手、翻转、暂停与插旗；新局参数、重新开始、认输、复盘记录、导出、成绩与操作说明进入设置。沙城突击沿用场景内暂停菜单。通行图标使用本地 Bootstrap SVG mask，保留 aria-label 与 title；不能由图形推断含义的操作、对手身份、分数及异常状态保留中文。去掉重复的操作区标题与黑白先后手说明；游戏名保留在公共页头，不再整体隐藏（见「游戏页标题与游戏列表」）。颜色继续由 home.css 双主题 tokens 提供，不改变存档结构。
Canonical UI Map：游戏布局/分层由 game-ui.js 与 game-ui.css 管理；棋盘业务及临时确认/计分/复盘仍由各游戏脚本管理；设置沿用原生 dialog，select 沿用系统原生菜单。计时游戏在设置打开时暂停，弹窗按键不会驱动背景游戏。
矮横屏例外：高度不足 480px 时，围棋的计分与落子确认面板改为内部滚动（go.css），保证棋盘不被压到尺寸下限；其余游戏与竖屏不受影响。
